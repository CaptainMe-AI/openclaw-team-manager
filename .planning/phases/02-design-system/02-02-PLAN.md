---
phase: 02-design-system
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - source/dashboard/app/frontend/components/ui/StatusDot.tsx
  - source/dashboard/app/frontend/components/ui/Badge.tsx
  - source/dashboard/app/frontend/components/ui/Card.tsx
  - source/dashboard/app/frontend/components/ui/Button.tsx
autonomous: true
requirements:
  - DSGN-03

must_haves:
  truths:
    - "StatusDot renders colored dots for all 8 statuses (active, success, idle, error, danger, warning, info, offline)"
    - "Badge renders status variant with optional dot and priority variant with P0-P3 colors"
    - "Card renders default variant with hover border transition and glow variant with box-shadow"
    - "Button renders primary (teal bg), secondary (surface bg), danger (red border), and ghost (transparent) variants"
    - "All components accept className prop for override and use cn() for class merging"
    - "Components use theme token classes (bg-surface, text-accent, etc.) not raw hex values"
  artifacts:
    - path: "source/dashboard/app/frontend/components/ui/StatusDot.tsx"
      provides: "Colored dot status indicator"
      exports: ["StatusDot"]
      min_lines: 20
    - path: "source/dashboard/app/frontend/components/ui/Badge.tsx"
      provides: "Status and priority label badges"
      exports: ["Badge"]
      min_lines: 30
    - path: "source/dashboard/app/frontend/components/ui/Card.tsx"
      provides: "Surface container component"
      exports: ["Card"]
      min_lines: 15
    - path: "source/dashboard/app/frontend/components/ui/Button.tsx"
      provides: "Action button with variants"
      exports: ["Button"]
      min_lines: 25
  key_links:
    - from: "source/dashboard/app/frontend/components/ui/Badge.tsx"
      to: "source/dashboard/app/frontend/components/ui/StatusDot.tsx"
      via: "import for dot prop"
      pattern: "import.*StatusDot.*from"
    - from: "source/dashboard/app/frontend/components/ui/StatusDot.tsx"
      to: "source/dashboard/app/frontend/lib/utils.ts"
      via: "cn() import"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "source/dashboard/app/frontend/components/ui/Card.tsx"
      to: "source/dashboard/app/frontend/lib/utils.ts"
      via: "cn() import"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Build the first four UI components: StatusDot, Badge, Card, and Button. These are the most-used components across all mockup screens and form the visual vocabulary for status indicators, containers, and actions.

Purpose: StatusDot and Badge are needed by nearly every screen (agent status, task priority, approval decisions). Card wraps every content panel. Button drives all user actions.
Output: Four typed React component files in app/frontend/components/ui/ with full variant support.
</objective>

<execution_context>
@/Users/vel/development/vlss/et/openclaw-team-manager/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vel/development/vlss/et/openclaw-team-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-UI-SPEC.md
@.planning/phases/02-design-system/02-01-SUMMARY.md

<interfaces>
<!-- cn() utility created by Plan 01 -->
From source/dashboard/app/frontend/lib/utils.ts:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<!-- Theme tokens available (from Plan 01 extended application.css) -->
Color tokens available as Tailwind classes:
- bg-background, bg-surface, bg-surface-hover, bg-accent, bg-accent-hover
- text-text-primary, text-text-secondary, text-accent
- border-border, border-danger, border-warning, border-success
- bg-danger, bg-warning, bg-success, bg-info
- bg-priority-0, bg-priority-1, bg-priority-2, bg-priority-3
- text-danger, text-warning, text-success, text-info
- Opacity modifiers: bg-success/10, border-success/20, etc.
- card-glow utility class (box-shadow)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusDot and Card components</name>
  <files>source/dashboard/app/frontend/components/ui/StatusDot.tsx, source/dashboard/app/frontend/components/ui/Card.tsx</files>
  <read_first>
    - source/dashboard/app/frontend/lib/utils.ts (cn() utility signature)
    - source/dashboard/app/frontend/styles/application.css (confirm theme tokens exist)
    - .planning/phases/02-design-system/02-UI-SPEC.md (StatusDot and Card visual contracts)
  </read_first>
  <action>
1. Create directory `source/dashboard/app/frontend/components/ui/` if it does not exist.

2. Create `source/dashboard/app/frontend/components/ui/StatusDot.tsx` (per D-01, D-03, D-08, D-13):

   Props interface (per UI-SPEC StatusDot section):
   - `status`: `"active" | "success" | "idle" | "error" | "danger" | "warning" | "info" | "offline"`, default `"active"`
   - `size`: `"sm" | "md"`, default `"sm"`
   - `pulse`: `boolean`, default `false`
   - `className`: `string | undefined`
   - Extends `React.HTMLAttributes<HTMLSpanElement>`

   Color mapping (use theme token classes per D-13):
   - active/success -> `bg-success`
   - idle -> `bg-text-secondary`
   - error/danger -> `bg-danger`
   - warning -> `bg-warning`
   - info -> `bg-info`
   - offline -> `bg-border`

   Size mapping:
   - sm: `w-1.5 h-1.5` (6px)
   - md: `w-2 h-2` (8px)

   Base classes: `inline-block rounded-full`
   Pulse: `animate-pulse` when pulse=true
   Use `cn()` for all class merging, pass `className` as last argument for overrides.
   Render as `<span>` element with `role="status"` and `aria-label` set to the status value for accessibility.

3. Create `source/dashboard/app/frontend/components/ui/Card.tsx` (per D-01, D-03, D-04, D-13):

   Props interface (per UI-SPEC Card section):
   - `variant`: `"default" | "glow"`, default `"default"`
   - `padding`: `boolean`, default `true`
   - `className`: `string | undefined`
   - `children`: `React.ReactNode` (required)
   - Extends `React.HTMLAttributes<HTMLDivElement>`

   Visual contract:
   - Base: `bg-surface rounded-lg border border-border`
   - Default variant adds: `hover:border-text-secondary transition-colors`
   - Glow variant adds: `card-glow overflow-hidden`
   - When padding=true: `p-6` (24px, per UI-SPEC)
   - Use `cn()` for class merging, `className` last for overrides
   - Render as `<div>` element with spread `...props`

4. Run TypeScript check:
   ```bash
   cd source/dashboard && npx tsc --noEmit
   ```
  </action>
  <verify>
    <automated>cd /Users/vel/development/vlss/et/openclaw-team-manager/source/dashboard && npx tsc --noEmit 2>&1 && grep -q "export function StatusDot" app/frontend/components/ui/StatusDot.tsx && grep -q "export function Card" app/frontend/components/ui/Card.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - StatusDot.tsx exists in source/dashboard/app/frontend/components/ui/
    - StatusDot.tsx contains `export function StatusDot(`
    - StatusDot.tsx contains `import { cn } from "@/lib/utils"`
    - StatusDot.tsx contains `bg-success` (for active/success status)
    - StatusDot.tsx contains `bg-danger` (for error/danger status)
    - StatusDot.tsx contains `bg-info` (for info status)
    - StatusDot.tsx contains `bg-border` (for offline status)
    - StatusDot.tsx contains `w-1.5 h-1.5` (sm size)
    - StatusDot.tsx contains `w-2 h-2` (md size)
    - StatusDot.tsx contains `animate-pulse`
    - Card.tsx exists in source/dashboard/app/frontend/components/ui/
    - Card.tsx contains `export function Card(`
    - Card.tsx contains `import { cn } from "@/lib/utils"`
    - Card.tsx contains `bg-surface rounded-lg border border-border`
    - Card.tsx contains `hover:border-text-secondary`
    - Card.tsx contains `card-glow`
    - Card.tsx contains `p-6`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>StatusDot renders colored dots in 8 statuses with 2 sizes and optional pulse. Card renders default and glow variants with optional padding. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create Badge and Button components</name>
  <files>source/dashboard/app/frontend/components/ui/Badge.tsx, source/dashboard/app/frontend/components/ui/Button.tsx</files>
  <read_first>
    - source/dashboard/app/frontend/components/ui/StatusDot.tsx (Badge imports StatusDot for dot prop)
    - source/dashboard/app/frontend/lib/utils.ts (cn() utility)
    - .planning/phases/02-design-system/02-UI-SPEC.md (Badge and Button visual contracts)
  </read_first>
  <action>
1. Create `source/dashboard/app/frontend/components/ui/Badge.tsx` (per D-01, D-03, D-08, D-13):

   Import StatusDot from `./StatusDot` (Badge uses StatusDot when `dot=true`).
   Import cn from `@/lib/utils`.

   Types:
   ```typescript
   type BadgeColor = "success" | "danger" | "warning" | "info" | "idle";
   type PriorityLevel = "p0" | "p1" | "p2" | "p3";
   ```

   Props interface (per UI-SPEC Badge section):
   - `variant`: `"status" | "priority"`, default `"status"`
   - `color`: `BadgeColor`, default `"success"` (used when variant="status")
   - `priority`: `PriorityLevel`, default `"p2"` (used when variant="priority")
   - `dot`: `boolean`, default `false` (show StatusDot before text)
   - `pulse`: `boolean`, default `false` (animate dot with pulse)
   - `className`: `string | undefined`
   - `children`: `React.ReactNode`
   - Extends `React.HTMLAttributes<HTMLSpanElement>`

   Color maps (Record types):
   Status colors:
   - success: `bg-success/10 text-success border-success/20`
   - danger: `bg-danger/10 text-danger border-danger/20`
   - warning: `bg-warning/10 text-warning border-warning/20`
   - info: `bg-info/10 text-info border-info/20`
   - idle: `bg-text-secondary/10 text-text-secondary border-text-secondary/20`

   Priority colors:
   - p0: `bg-priority-0/10 text-priority-0 border-priority-0/20`
   - p1: `bg-priority-1/10 text-priority-1 border-priority-1/20`
   - p2: `bg-priority-2/10 text-priority-2 border-priority-2/20`
   - p3: `bg-priority-3/10 text-priority-3 border-priority-3/20`

   Base classes: `inline-flex items-center gap-1 px-2 py-1 rounded text-[10px] font-normal border`

   When `dot=true` and variant="status", render `<StatusDot status={color} size="sm" pulse={pulse} />` before children.
   Use `cn()` for all class merging.

2. Create `source/dashboard/app/frontend/components/ui/Button.tsx` (per D-01, D-03, D-08, D-13):

   Import cn from `@/lib/utils`.

   Props interface (per UI-SPEC Button section):
   - `variant`: `"primary" | "secondary" | "danger" | "ghost"`, default `"primary"`
   - `size`: `"sm" | "md" | "lg"`, default `"md"`
   - `glow`: `boolean`, default `false` (add glow shadow, primary variant only)
   - `className`: `string | undefined`
   - `children`: `React.ReactNode`
   - Extends `React.ButtonHTMLAttributes<HTMLButtonElement>`

   Variant classes:
   - primary: `bg-accent text-background hover:bg-accent-hover`
   - secondary: `bg-surface border border-border text-text-primary hover:bg-surface-hover`
   - danger: `bg-transparent border border-danger text-danger hover:bg-danger/10`
   - ghost: `bg-transparent text-text-secondary hover:bg-surface-hover hover:text-text-primary`

   Size classes:
   - sm: `px-2 py-1 text-xs gap-1`
   - md: `px-3 py-2 text-sm gap-2`
   - lg: `px-4 py-2 text-sm gap-2`

   Base classes (all variants): `inline-flex items-center justify-center rounded-md text-sm font-semibold transition-colors`
   Focus: `focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-background`
   Active: `active:scale-[0.98]`
   Disabled: `disabled:opacity-50 disabled:cursor-not-allowed disabled:pointer-events-none`
   Glow (when glow=true AND variant="primary"): `shadow-[0_0_10px_rgba(0,212,170,0.3)]`

   Render as `<button>` with `type` defaulting to `"button"` (not "submit") to prevent accidental form submissions.
   Spread `...props` onto the button element.

3. Run TypeScript check:
   ```bash
   cd source/dashboard && npx tsc --noEmit
   ```
  </action>
  <verify>
    <automated>cd /Users/vel/development/vlss/et/openclaw-team-manager/source/dashboard && npx tsc --noEmit 2>&1 && grep -q "export function Badge" app/frontend/components/ui/Badge.tsx && grep -q "export function Button" app/frontend/components/ui/Button.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Badge.tsx exists in source/dashboard/app/frontend/components/ui/
    - Badge.tsx contains `export function Badge(`
    - Badge.tsx contains `import { cn } from "@/lib/utils"`
    - Badge.tsx contains `import { StatusDot } from "./StatusDot"`
    - Badge.tsx contains `bg-success/10 text-success border-success/20`
    - Badge.tsx contains `bg-priority-0/10 text-priority-0 border-priority-0/20`
    - Badge.tsx contains `text-[10px]`
    - Button.tsx exists in source/dashboard/app/frontend/components/ui/
    - Button.tsx contains `export function Button(`
    - Button.tsx contains `import { cn } from "@/lib/utils"`
    - Button.tsx contains `bg-accent text-background hover:bg-accent-hover`
    - Button.tsx contains `bg-transparent border border-danger text-danger`
    - Button.tsx contains `focus:ring-2 focus:ring-accent`
    - Button.tsx contains `shadow-[0_0_10px_rgba(0,212,170,0.3)]`
    - Button.tsx contains `disabled:opacity-50`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Badge renders status and priority variants with dot support. Button renders 4 variants in 3 sizes with glow, focus, active, and disabled states. Both compile cleanly.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd source/dashboard && npx tsc --noEmit` -- zero errors
2. All 4 component files exist in app/frontend/components/ui/
3. Each component imports cn() from @/lib/utils (verifies Vite alias works at TypeScript level)
4. Badge imports StatusDot (verifies cross-component import)
5. No raw hex values in any component file (per D-13)
</verification>

<success_criteria>
- StatusDot: 8 statuses, 2 sizes, pulse animation, renders as colored dot
- Badge: status variant (5 colors + dot), priority variant (P0-P3), renders as labeled pill
- Card: default (hover border) and glow (box-shadow) variants, optional padding
- Button: 4 variants, 3 sizes, glow on primary, disabled state, focus ring
- All components use cn() and theme tokens exclusively
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-02-SUMMARY.md`
</output>
