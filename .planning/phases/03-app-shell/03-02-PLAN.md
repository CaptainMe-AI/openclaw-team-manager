---
phase: 03-app-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - source/dashboard/app/frontend/components/layout/Sidebar.tsx
  - source/dashboard/app/frontend/components/layout/TopBar.tsx
  - source/dashboard/app/frontend/components/layout/Breadcrumb.tsx
  - source/dashboard/app/frontend/components/layout/SearchInput.tsx
  - source/dashboard/app/frontend/components/layout/NotificationBell.tsx
  - source/dashboard/app/frontend/components/layout/UserMenu.tsx
  - source/dashboard/app/frontend/components/layout/AppShell.tsx
  - source/dashboard/app/frontend/router.tsx
autonomous: true
requirements:
  - SHEL-01
  - SHEL-02
  - SHEL-03
  - SHEL-04

must_haves:
  truths:
    - "Sidebar shows 6 navigation links with Font Awesome icons and labels"
    - "Active sidebar link has teal left-border, teal text, and surface-hover background"
    - "Dashboard NavLink only highlights on exact / path (not on /agents, /tasks, etc.)"
    - "Approvals nav item shows a hardcoded badge with count 3"
    - "Sidebar collapses on mobile and shows as a slide-in drawer with backdrop"
    - "Mobile sidebar closes when a nav link is clicked"
    - "Top bar shows breadcrumb as Command Center > {Page Name}"
    - "Search input focuses when user presses / key (unless already in an input field)"
    - "Notification bell displays with a teal unread dot indicator"
    - "Clicking user avatar opens a dropdown with Profile, Settings, Sign Out"
    - "Dropdown closes on outside click or Escape key"
    - "Sign Out destroys Devise session and redirects to sign-in page"
  artifacts:
    - path: "source/dashboard/app/frontend/components/layout/Sidebar.tsx"
      provides: "Sidebar navigation with 6 NavLink items, icons, active states, mobile drawer"
      exports: ["Sidebar"]
    - path: "source/dashboard/app/frontend/components/layout/TopBar.tsx"
      provides: "Top navigation bar assembling breadcrumb, search, bell, user menu"
      exports: ["TopBar"]
    - path: "source/dashboard/app/frontend/components/layout/Breadcrumb.tsx"
      provides: "Route-derived breadcrumb: Command Center > Page Name"
      exports: ["Breadcrumb"]
    - path: "source/dashboard/app/frontend/components/layout/SearchInput.tsx"
      provides: "Search input with / keyboard shortcut"
      exports: ["SearchInput"]
    - path: "source/dashboard/app/frontend/components/layout/NotificationBell.tsx"
      provides: "Bell icon with static unread dot"
      exports: ["NotificationBell"]
    - path: "source/dashboard/app/frontend/components/layout/UserMenu.tsx"
      provides: "Avatar button + dropdown with Profile, Settings, Sign Out"
      exports: ["UserMenu"]
    - path: "source/dashboard/app/frontend/components/layout/AppShell.tsx"
      provides: "Root layout: sidebar + top bar + content outlet"
      exports: ["AppShell"]
  key_links:
    - from: "source/dashboard/app/frontend/router.tsx"
      to: "source/dashboard/app/frontend/components/layout/AppShell.tsx"
      via: "root route element"
      pattern: "element:.*AppShell"
    - from: "source/dashboard/app/frontend/components/layout/AppShell.tsx"
      to: "source/dashboard/app/frontend/components/layout/Sidebar.tsx"
      via: "import and render"
      pattern: "import.*Sidebar"
    - from: "source/dashboard/app/frontend/components/layout/AppShell.tsx"
      to: "source/dashboard/app/frontend/components/layout/TopBar.tsx"
      via: "import and render"
      pattern: "import.*TopBar"
    - from: "source/dashboard/app/frontend/components/layout/Sidebar.tsx"
      to: "source/dashboard/app/frontend/stores/uiStore.ts"
      via: "Zustand useUIStore for mobile close"
      pattern: "useUIStore"
    - from: "source/dashboard/app/frontend/components/layout/UserMenu.tsx"
      to: "source/dashboard/app/frontend/lib/csrf.ts"
      via: "getCsrfToken for sign-out"
      pattern: "getCsrfToken"
---

<objective>
Build all navigation chrome components: Sidebar (with NavLink items, icons, active states, mobile drawer), TopBar (with Breadcrumb, SearchInput, NotificationBell, UserMenu), and AppShell (root layout composing sidebar + top bar + content outlet). Wire AppShell into the router as the root layout element.

Purpose: Deliver the persistent navigation shell that wraps all screens, enabling operators to navigate between sections and sign out. This is the structural skeleton all future screens render inside.

Output: Fully functional app shell with sidebar navigation, top bar, user menu, and responsive mobile drawer.
</objective>

<execution_context>
@/Users/vel/development/vlss/et/openclaw-team-manager/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vel/development/vlss/et/openclaw-team-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-app-shell/03-CONTEXT.md
@.planning/phases/03-app-shell/03-RESEARCH.md
@.planning/phases/03-app-shell/03-UI-SPEC.md
@.planning/phases/03-app-shell/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From source/dashboard/app/frontend/stores/uiStore.ts:
```typescript
import { create } from "zustand";

interface UIState {
  sidebarOpen: boolean;
  toggleSidebar: () => void;
  closeSidebar: () => void;
}

export const useUIStore = create<UIState>((set) => ({
  sidebarOpen: false,
  toggleSidebar: () => set((state) => ({ sidebarOpen: !state.sidebarOpen })),
  closeSidebar: () => set({ sidebarOpen: false }),
}));
```

From source/dashboard/app/frontend/lib/csrf.ts:
```typescript
export function getCsrfToken(): string {
  const meta = document.querySelector('meta[name="csrf-token"]');
  return meta?.getAttribute("content") ?? "";
}
```

From source/dashboard/app/frontend/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

From source/dashboard/app/frontend/components/ui/Badge.tsx:
```typescript
interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: "status" | "priority";
  color?: BadgeColor;
  priority?: PriorityLevel;
  dot?: boolean;
  pulse?: boolean;
  children: React.ReactNode;
}
export function Badge({ variant, color, priority, dot, pulse, className, children, ...props }: BadgeProps)
```

From source/dashboard/app/frontend/router.tsx (Plan 01 temporary layout):
```typescript
import { createBrowserRouter, Outlet } from "react-router";
// ... page imports ...

function RootLayout() {
  return (
    <div className="min-h-screen bg-background">
      <Outlet />
    </div>
  );
}

export const router = createBrowserRouter([
  {
    path: "/",
    element: <RootLayout />,
    children: [
      { index: true, element: <DashboardPage /> },
      // ... other routes
    ],
  },
]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sidebar, Breadcrumb, SearchInput, and NotificationBell components</name>
  <files>
    source/dashboard/app/frontend/components/layout/Sidebar.tsx,
    source/dashboard/app/frontend/components/layout/Breadcrumb.tsx,
    source/dashboard/app/frontend/components/layout/SearchInput.tsx,
    source/dashboard/app/frontend/components/layout/NotificationBell.tsx
  </files>
  <read_first>
    source/dashboard/app/frontend/stores/uiStore.ts,
    source/dashboard/app/frontend/lib/utils.ts,
    source/dashboard/app/frontend/components/ui/Badge.tsx,
    source/dashboard/app/frontend/components/ui/Input.tsx,
    .planning/phases/03-app-shell/03-UI-SPEC.md,
    .planning/phases/03-app-shell/03-RESEARCH.md
  </read_first>
  <action>
Create the `source/dashboard/app/frontend/components/layout/` directory.

**Step 1: Create `Sidebar.tsx`.**
Per D-01, D-02, D-03, D-04, UI-SPEC Sidebar Navigation section.

Navigation items data array (per RESEARCH.md):
```typescript
import { faChartLine, faRobot, faListCheck, faChartPie, faShieldCheck, faGear, faXmark } from "@fortawesome/free-solid-svg-icons";
import type { IconDefinition } from "@fortawesome/fontawesome-svg-core";

interface NavItem {
  label: string;
  path: string;
  icon: IconDefinition;
  badge?: number;
}

const navItems: NavItem[] = [
  { label: "Dashboard", path: "/",          icon: faChartLine },
  { label: "Agents",    path: "/agents",    icon: faRobot },
  { label: "Tasks",     path: "/tasks",     icon: faListCheck },
  { label: "Usage",     path: "/usage",     icon: faChartPie },
  { label: "Approvals", path: "/approvals", icon: faShieldCheck, badge: 3 },
  { label: "Settings",  path: "/settings",  icon: faGear },
];
```

Component implementation:
- Import `NavLink` from `"react-router"` and `useLocation` from `"react-router"`.
- Import `FontAwesomeIcon` from `@fortawesome/react-fontawesome`.
- Import `Badge` from `@/components/ui`.
- Import `useUIStore` from `@/stores/uiStore`.
- Import `cn` from `@/lib/utils`.

**Desktop sidebar** (visible at `lg:` breakpoint and above):
- Container: `hidden lg:flex lg:flex-col lg:w-64 lg:fixed lg:inset-y-0 bg-surface border-r border-border`.
- Logo area at top: `px-4 py-4 flex items-center gap-2`. FontAwesomeIcon `faRobot` with `className="w-4 h-4 text-accent"` + "OpenClaw" text with `className="text-sm font-semibold text-text-primary"`.
- Nav section: `<nav className="flex-1 px-2 py-4">`. Each nav item is a `<NavLink>` element.
- NavLink className function receives `{ isActive }`:
  - Base classes: `"flex items-center gap-3 px-4 py-2 text-sm rounded-md transition-colors"`.
  - Active: `"bg-surface-hover text-accent border-l-2 border-accent"`.
  - Inactive: `"text-text-secondary hover:bg-surface-hover hover:text-text-primary"`.
- Dashboard NavLink MUST use `end` prop: `<NavLink to="/" end ...>`. Per RESEARCH.md Pitfall 4.
- All other NavLinks do NOT use `end`.
- FontAwesomeIcon in each nav item: `className="w-4 h-4"` with `aria-hidden="true"`.
- Approvals nav item: After the label text, render `<Badge color="info" className="ml-auto">{item.badge}</Badge>` when `item.badge` is defined. Per D-04.

**Mobile sidebar drawer** (visible below `lg:` breakpoint):
- Read `sidebarOpen` and `closeSidebar` from `useUIStore`.
- Watch `location.pathname` via `useEffect` and call `closeSidebar()` on change. Per RESEARCH.md Pitfall 5.
- Render only when `sidebarOpen` is true (or use CSS transform for animation).
- Backdrop overlay: `<div className="fixed inset-0 bg-black/50 z-40 lg:hidden" onClick={closeSidebar} aria-hidden="true" />`.
- Drawer: `<div className="fixed inset-y-0 left-0 w-64 bg-surface border-r border-border z-50 lg:hidden transform transition-transform duration-200 ease-out">`.
  - When open: `translate-x-0`. When closed: `-translate-x-full`.
  - Better approach for animation: Always render both backdrop and drawer, but toggle visibility via transform and opacity. Backdrop: `opacity-0 pointer-events-none` when closed, `opacity-100 pointer-events-auto` when open. Drawer: `translate-x(-100%)` when closed, `translate-x(0)` when open.
- Close button in drawer top-right: FontAwesomeIcon `faXmark` with `className="w-5 h-5"`, positioned `absolute top-4 right-4`, `text-text-secondary hover:text-text-primary`.
- Same logo and nav items as desktop sidebar (reuse the same rendering logic in a shared function or render both desktop and mobile variants).

**Step 2: Create `Breadcrumb.tsx`.**
Per D-05, UI-SPEC TopBar section.

```typescript
import { useLocation } from "react-router";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faChevronRight } from "@fortawesome/free-solid-svg-icons";

const routeNames: Record<string, string> = {
  "/": "Dashboard",
  "/agents": "Agents",
  "/tasks": "Tasks",
  "/usage": "Usage",
  "/approvals": "Approvals",
  "/settings": "Settings",
};

export function Breadcrumb() {
  const { pathname } = useLocation();
  const pageName = routeNames[pathname] ?? "Unknown";

  return (
    <nav className="flex items-center gap-2 text-sm" aria-label="Breadcrumb">
      <span className="text-text-secondary">Command Center</span>
      <FontAwesomeIcon icon={faChevronRight} className="w-2.5 h-2.5 text-text-secondary" />
      <span className="text-text-primary font-semibold">{pageName}</span>
    </nav>
  );
}
```

Separator icon is `faChevronRight` at ~10px (`w-2.5 h-2.5`) per UI-SPEC. Root text "Command Center" is `text-text-secondary`. Active page name is `text-text-primary font-semibold`.

**Step 3: Create `SearchInput.tsx`.**
Per D-06, UI-SPEC TopBar section, RESEARCH.md Pattern 6.

- Render a search input (NOT using the existing Input component -- build a simpler search-specific component since the existing Input has label/error/helper features not needed here).
- Container: `relative`. Hidden on mobile: `hidden sm:block`.
- Width: `w-48 sm:w-48 lg:w-64` (48 on tablet, 64 on desktop per UI-SPEC responsive table).
- Input element: `bg-background border border-border rounded-md py-2 pl-9 pr-3 text-sm text-text-primary placeholder-text-secondary focus:outline-none focus:border-accent focus:ring-1 focus:ring-accent transition-colors`.
- Left icon: `faMagnifyingGlass` in a `span` with `absolute left-3 top-1/2 -translate-y-1/2 text-text-secondary`. Icon sized `w-3.5 h-3.5`.
- Placeholder text: `"Search agents, tasks, or approvals (Press '/')"`.
- `aria-label="Search"`.
- Keyboard shortcut: `useRef<HTMLInputElement>` + `useEffect` with global `keydown` listener. On `"/"` key, check `e.target` tagName is NOT INPUT, TEXTAREA, or SELECT, and `contentEditable` is not `"true"`. If safe, call `e.preventDefault()` then `inputRef.current?.focus()`. Per RESEARCH.md Pattern 6, Pitfall 3.

**Step 4: Create `NotificationBell.tsx`.**
Per D-07, UI-SPEC TopBar section.

Static visual component, no click handler, no dropdown. Per D-07 this is a visual placeholder only.

```typescript
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faBell } from "@fortawesome/free-solid-svg-icons";

export function NotificationBell() {
  return (
    <button
      type="button"
      className="relative p-2 text-text-secondary hover:text-text-primary transition-colors"
      aria-label="Notifications"
    >
      <FontAwesomeIcon icon={faBell} className="w-5 h-5" />
      {/* Unread dot indicator */}
      <span className="absolute top-1.5 right-1.5 w-2 h-2 rounded-full bg-accent" />
    </button>
  );
}
```

Use a `<button>` element for semantic correctness even though there's no click behavior yet. The unread dot is `w-2 h-2 rounded-full bg-accent` absolutely positioned at `top-1.5 right-1.5`.
  </action>
  <verify>
    <automated>cd source/dashboard && test -f app/frontend/components/layout/Sidebar.tsx && test -f app/frontend/components/layout/Breadcrumb.tsx && test -f app/frontend/components/layout/SearchInput.tsx && test -f app/frontend/components/layout/NotificationBell.tsx && grep -q "NavLink" app/frontend/components/layout/Sidebar.tsx && grep -q "faChevronRight" app/frontend/components/layout/Breadcrumb.tsx && grep -q "faMagnifyingGlass" app/frontend/components/layout/SearchInput.tsx && grep -q "faBell" app/frontend/components/layout/NotificationBell.tsx && echo "all layout components OK"</automated>
  </verify>
  <acceptance_criteria>
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `export function Sidebar` or `export const Sidebar`
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `NavLink` import from `"react-router"`
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `faChartLine` and `faRobot` and `faListCheck` and `faChartPie` and `faShieldCheck` and `faGear`
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `isActive`
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `bg-surface-hover text-accent border-l-2 border-accent` (active state classes)
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `text-text-secondary hover:bg-surface-hover hover:text-text-primary` (inactive state classes)
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `end` prop on Dashboard NavLink (the word "end" as a JSX prop)
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `badge: 3` for Approvals
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `useUIStore` import
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `closeSidebar`
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `bg-black/50` (backdrop overlay)
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `faXmark` (mobile close button)
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `OpenClaw` text
    - source/dashboard/app/frontend/components/layout/Sidebar.tsx contains `faRobot` import (logo icon)
    - source/dashboard/app/frontend/components/layout/Breadcrumb.tsx contains `export function Breadcrumb`
    - source/dashboard/app/frontend/components/layout/Breadcrumb.tsx contains `Command Center`
    - source/dashboard/app/frontend/components/layout/Breadcrumb.tsx contains `useLocation`
    - source/dashboard/app/frontend/components/layout/Breadcrumb.tsx contains `faChevronRight`
    - source/dashboard/app/frontend/components/layout/Breadcrumb.tsx contains `font-semibold` on active page name
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains `export function SearchInput`
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains the exact placeholder text `Search agents, tasks, or approvals (Press '/')`
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains `faMagnifyingGlass`
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains `e.key === "/"` (keyboard shortcut listener)
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains `INPUT` and `TEXTAREA` and `SELECT` (tag name checks to prevent shortcut in form fields)
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains `e.preventDefault()`
    - source/dashboard/app/frontend/components/layout/SearchInput.tsx contains `hidden sm:block` (hidden on mobile)
    - source/dashboard/app/frontend/components/layout/NotificationBell.tsx contains `export function NotificationBell`
    - source/dashboard/app/frontend/components/layout/NotificationBell.tsx contains `faBell`
    - source/dashboard/app/frontend/components/layout/NotificationBell.tsx contains `w-2 h-2 rounded-full bg-accent` (unread dot)
    - source/dashboard/app/frontend/components/layout/NotificationBell.tsx contains `aria-label` attribute
  </acceptance_criteria>
  <done>
    Sidebar renders 6 NavLink items with FA icons, teal active state with left-border, mobile drawer with backdrop, hardcoded approval badge. Breadcrumb shows "Command Center > {Page Name}". SearchInput focuses on "/" keypress. NotificationBell shows static bell with unread dot.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create UserMenu, TopBar, and AppShell components; wire AppShell into router</name>
  <files>
    source/dashboard/app/frontend/components/layout/UserMenu.tsx,
    source/dashboard/app/frontend/components/layout/TopBar.tsx,
    source/dashboard/app/frontend/components/layout/AppShell.tsx,
    source/dashboard/app/frontend/router.tsx
  </files>
  <read_first>
    source/dashboard/app/frontend/router.tsx,
    source/dashboard/app/frontend/lib/csrf.ts,
    source/dashboard/app/frontend/stores/uiStore.ts,
    source/dashboard/app/frontend/components/layout/Sidebar.tsx,
    source/dashboard/app/frontend/components/layout/Breadcrumb.tsx,
    source/dashboard/app/frontend/components/layout/SearchInput.tsx,
    source/dashboard/app/frontend/components/layout/NotificationBell.tsx,
    .planning/phases/03-app-shell/03-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Create `UserMenu.tsx`.**
Per D-08, D-09, UI-SPEC User Menu Dropdown section, RESEARCH.md Pattern 7.

Component structure:
- Import `useState`, `useRef`, `useEffect`, `useCallback` from React.
- Import `FontAwesomeIcon` from `@fortawesome/react-fontawesome`.
- Import `faUser`, `faGear`, `faRightFromBracket` from `@fortawesome/free-solid-svg-icons`.
- Import `useNavigate` from `"react-router"`.
- Import `getCsrfToken` from `@/lib/csrf`.
- Import `cn` from `@/lib/utils`.

State: `const [isOpen, setIsOpen] = useState(false)`.
Ref: `const menuRef = useRef<HTMLDivElement>(null)`.

**Click-outside and Escape close** (inline, per RESEARCH.md Pattern 7):
```typescript
const closeMenu = useCallback(() => setIsOpen(false), []);

useEffect(() => {
  if (!isOpen) return;

  function handleClick(e: MouseEvent) {
    if (menuRef.current && !menuRef.current.contains(e.target as Node)) {
      closeMenu();
    }
  }
  function handleEscape(e: KeyboardEvent) {
    if (e.key === "Escape") closeMenu();
  }
  document.addEventListener("mousedown", handleClick);
  document.addEventListener("keydown", handleEscape);
  return () => {
    document.removeEventListener("mousedown", handleClick);
    document.removeEventListener("keydown", handleEscape);
  };
}, [isOpen, closeMenu]);
```

**Sign Out handler** (per RESEARCH.md Pattern 5, Pitfall 6):
```typescript
async function handleSignOut() {
  const token = getCsrfToken(); // Read fresh on each call, not cached
  await fetch("/users/sign_out", {
    method: "DELETE",
    headers: {
      "X-CSRF-Token": token,
      "Content-Type": "application/json",
    },
    credentials: "same-origin",
  });
  window.location.href = "/users/sign_in"; // Full page nav, NOT React Router
}
```
CRITICAL: Use `window.location.href` (not `navigate()`) so the browser loads the Devise login page via Rails, not the SPA. Per RESEARCH.md Pitfall 2.

**Profile and Settings links**: Both navigate to `/settings` via `navigate("/settings")` then `closeMenu()`. Per D-08.

**Render structure:**
- Wrapper `<div ref={menuRef} className="relative">`.
- Trigger button: `<button onClick={() => setIsOpen(!isOpen)} className="w-8 h-8 rounded-full bg-surface-hover flex items-center justify-center" aria-expanded={isOpen} aria-haspopup="true">`.
  - Inside: `<FontAwesomeIcon icon={faUser} className="w-4 h-4 text-text-secondary" />`.
- Dropdown (rendered inline, NOT portal, per UI-SPEC and RESEARCH.md Anti-Patterns):
  - Container: `{isOpen && <div className="absolute right-0 top-full mt-2 w-56 bg-surface border border-border rounded-lg shadow-lg z-50">`.
  - Header: `<div className="px-4 py-3 border-b border-border">`. Display name: `<p className="text-sm font-semibold text-text-primary">Admin User</p>`. Email: `<p className="text-xs text-text-secondary">admin@openclaw.local</p>`. (Hardcoded for now -- real user data from Devise comes in a future phase.)
  - Menu items: `<div className="py-1">`.
    - "Profile": `<button onClick={...} className="w-full flex items-center gap-3 px-4 py-2 text-sm text-text-secondary hover:bg-surface-hover hover:text-text-primary transition-colors">`. Icon: `faUser`. Navigates to `/settings`.
    - "Settings": Same styling. Icon: `faGear`. Navigates to `/settings`.
  - Divider: `<div className="border-t border-border" />`.
  - "Sign Out": `<button onClick={handleSignOut} className="w-full flex items-center gap-3 px-4 py-2 text-sm text-danger hover:bg-danger/10 transition-colors">`. Icon: `faRightFromBracket`.
  - Close `</div>`.

**Step 2: Create `TopBar.tsx`.**
Per UI-SPEC TopBar section. Assembles Breadcrumb, SearchInput, NotificationBell, and UserMenu.

```typescript
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faBars } from "@fortawesome/free-solid-svg-icons";
import { useUIStore } from "@/stores/uiStore";
import { Breadcrumb } from "./Breadcrumb";
import { SearchInput } from "./SearchInput";
import { NotificationBell } from "./NotificationBell";
import { UserMenu } from "./UserMenu";

export function TopBar() {
  const toggleSidebar = useUIStore((state) => state.toggleSidebar);

  return (
    <header className="h-16 bg-surface border-b border-border px-4 lg:px-6 flex items-center justify-between">
      {/* Left section */}
      <div className="flex items-center gap-4">
        {/* Mobile hamburger - hidden on desktop */}
        <button
          type="button"
          onClick={toggleSidebar}
          className="lg:hidden p-2 text-text-secondary hover:text-text-primary transition-colors"
          aria-label="Toggle navigation"
        >
          <FontAwesomeIcon icon={faBars} className="w-5 h-5" />
        </button>
        <Breadcrumb />
      </div>
      {/* Right section */}
      <div className="flex items-center gap-2">
        <SearchInput />
        <NotificationBell />
        <UserMenu />
      </div>
    </header>
  );
}
```

TopBar is `h-16` (64px). Background `bg-surface`. Bottom border `border-b border-border`. Padding `px-4` on mobile, `lg:px-6` on desktop. Flex row with `justify-between`.

Left side: hamburger button (visible below `lg:`, hidden at `lg:` and above with `lg:hidden`) + Breadcrumb.
Right side: SearchInput + NotificationBell + UserMenu with `gap-2`.

Hamburger: `faBars` icon at `w-5 h-5`, calls `toggleSidebar` from `useUIStore`.

**Step 3: Create `AppShell.tsx`.**
Per D-13, UI-SPEC Component Inventory, RESEARCH.md Pattern 1.

Root layout combining sidebar + top bar + content outlet:
```typescript
import { Outlet } from "react-router";
import { Sidebar } from "./Sidebar";
import { TopBar } from "./TopBar";

export function AppShell() {
  return (
    <div className="flex h-screen bg-background">
      <Sidebar />
      <div className="flex flex-1 flex-col lg:pl-64">
        <TopBar />
        <main className="flex-1 overflow-y-auto p-4 lg:p-6">
          <Outlet />
        </main>
      </div>
    </div>
  );
}
```

Key layout details:
- `flex h-screen bg-background` -- full viewport height, dark background.
- Desktop sidebar is `fixed` (from Sidebar component), so main content uses `lg:pl-64` to offset for the fixed sidebar width.
- Mobile has no padding offset (sidebar is an overlay drawer).
- Main content: `flex-1 overflow-y-auto` for scrollable content area. Padding: `p-4` on mobile, `lg:p-6` on desktop per UI-SPEC responsive table.
- `<Outlet />` renders the child route component (placeholder pages from Plan 01).

**Step 4: Update `router.tsx`.**
Replace the temporary `RootLayout` with the `AppShell` component:
- Remove the `RootLayout` function.
- Remove the `Outlet` import (AppShell handles it internally).
- Add import: `import { AppShell } from "@/components/layout/AppShell";`.
- Change root route element from `<RootLayout />` to `<AppShell />`.
- Keep all child route definitions unchanged.

Updated router structure:
```typescript
import { createBrowserRouter } from "react-router";
import { AppShell } from "@/components/layout/AppShell";
import { DashboardPage } from "@/components/pages/DashboardPage";
import { AgentsPage } from "@/components/pages/AgentsPage";
import { TasksPage } from "@/components/pages/TasksPage";
import { UsagePage } from "@/components/pages/UsagePage";
import { ApprovalsPage } from "@/components/pages/ApprovalsPage";
import { SettingsPage } from "@/components/pages/SettingsPage";

export const router = createBrowserRouter([
  {
    path: "/",
    element: <AppShell />,
    children: [
      { index: true, element: <DashboardPage /> },
      { path: "agents", element: <AgentsPage /> },
      { path: "tasks", element: <TasksPage /> },
      { path: "usage", element: <UsagePage /> },
      { path: "approvals", element: <ApprovalsPage /> },
      { path: "settings", element: <SettingsPage /> },
    ],
  },
]);
```
  </action>
  <verify>
    <automated>cd source/dashboard && test -f app/frontend/components/layout/UserMenu.tsx && test -f app/frontend/components/layout/TopBar.tsx && test -f app/frontend/components/layout/AppShell.tsx && grep -q "AppShell" app/frontend/router.tsx && grep -q "getCsrfToken" app/frontend/components/layout/UserMenu.tsx && grep -q "faBars" app/frontend/components/layout/TopBar.tsx && grep -q "Outlet" app/frontend/components/layout/AppShell.tsx && npx tsc --noEmit && echo "all OK"</automated>
  </verify>
  <acceptance_criteria>
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `export function UserMenu`
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `getCsrfToken` import from `@/lib/csrf`
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `DELETE` and `/users/sign_out` (sign-out fetch)
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `X-CSRF-Token` header
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `window.location.href = "/users/sign_in"` (full page redirect, NOT navigate)
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `aria-expanded` and `aria-haspopup`
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `Escape` (escape key close)
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `mousedown` (outside click listener)
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `Admin User` and `admin@openclaw.local` (hardcoded user info)
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `Profile` and `Settings` and `Sign Out` menu items
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `faUser` and `faGear` and `faRightFromBracket` icon imports
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `text-danger` (sign out styling)
    - source/dashboard/app/frontend/components/layout/UserMenu.tsx contains `bg-danger/10` (sign out hover)
    - source/dashboard/app/frontend/components/layout/TopBar.tsx contains `export function TopBar`
    - source/dashboard/app/frontend/components/layout/TopBar.tsx contains `faBars` import (hamburger)
    - source/dashboard/app/frontend/components/layout/TopBar.tsx contains `toggleSidebar`
    - source/dashboard/app/frontend/components/layout/TopBar.tsx contains `lg:hidden` (hamburger hidden on desktop)
    - source/dashboard/app/frontend/components/layout/TopBar.tsx contains `h-16` (64px height)
    - source/dashboard/app/frontend/components/layout/TopBar.tsx contains `bg-surface` and `border-b border-border`
    - source/dashboard/app/frontend/components/layout/TopBar.tsx imports and renders `Breadcrumb`, `SearchInput`, `NotificationBell`, `UserMenu`
    - source/dashboard/app/frontend/components/layout/AppShell.tsx contains `export function AppShell`
    - source/dashboard/app/frontend/components/layout/AppShell.tsx contains `Outlet` import from `"react-router"`
    - source/dashboard/app/frontend/components/layout/AppShell.tsx imports `Sidebar` and `TopBar`
    - source/dashboard/app/frontend/components/layout/AppShell.tsx contains `lg:pl-64` (offset for fixed sidebar)
    - source/dashboard/app/frontend/components/layout/AppShell.tsx contains `p-4 lg:p-6` (responsive padding)
    - source/dashboard/app/frontend/components/layout/AppShell.tsx contains `overflow-y-auto` (scrollable content)
    - source/dashboard/app/frontend/router.tsx contains `import { AppShell }` (not RootLayout)
    - source/dashboard/app/frontend/router.tsx contains `element: <AppShell />`
    - source/dashboard/app/frontend/router.tsx does NOT contain `function RootLayout` (temporary layout removed)
    - `cd source/dashboard && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    UserMenu renders avatar with click-triggered dropdown (Profile, Settings, Sign Out with CSRF). TopBar assembles hamburger, breadcrumb, search, bell, and user menu. AppShell composes sidebar + top bar + Outlet. Router uses AppShell as root layout. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. `cd source/dashboard && npx tsc --noEmit` -- TypeScript compiles without errors
2. AppShell renders sidebar on desktop, drawer on mobile
3. NavLink active states use correct Tailwind classes (teal border + text)
4. Dashboard NavLink uses `end` prop
5. Breadcrumb shows "Command Center > {Page Name}" for each route
6. Search input focuses on "/" key, ignores "/" when in input fields
7. Notification bell has unread dot
8. User menu opens on click, closes on outside click/Escape
9. Sign Out calls DELETE /users/sign_out with CSRF token
10. Router uses AppShell (not temporary RootLayout)
</verification>

<success_criteria>
- Sidebar renders all 6 nav items with correct icons and labels
- Active link has `bg-surface-hover text-accent border-l-2 border-accent` styling
- Dashboard NavLink uses `end` prop to prevent false active state
- Approvals shows Badge with hardcoded count "3"
- Mobile sidebar slides in as drawer with backdrop, closes on nav click
- Breadcrumb shows "Command Center > {Page Name}" per current route
- Search input has "/" keyboard shortcut that skips form fields
- Notification bell shows with teal unread dot
- User menu has Profile, Settings (both to /settings), Sign Out (DELETE session + full page redirect)
- Dropdown has close-on-outside-click and close-on-Escape
- AppShell composes sidebar + top bar + outlet with responsive padding
- Router uses AppShell as root layout element
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-shell/03-02-SUMMARY.md`
</output>
