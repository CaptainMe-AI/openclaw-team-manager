---
phase: 06-task-board
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx
  - source/dashboard/app/frontend/components/tasks/TaskListView.tsx
  - source/dashboard/app/frontend/components/pages/TasksPage.tsx
autonomous: true
requirements: [NTSK-01, NTSK-02, NTSK-03, NTSK-04]

must_haves:
  truths:
    - "Clicking '+ New Task' opens a modal with task name input, agent dropdown, description textarea, attachment placeholder, and priority button group"
    - "Form defaults priority to Medium (2) and validates name, agent, and description as required"
    - "Submitting valid form creates task via API, shows success toast, and closes modal"
    - "Clicking attachment area shows toast 'File attachments coming soon'"
    - "Task list view shows a sortable table with task data matching AgentTable pattern"
    - "TasksPage composes all components: header, filters, priority legend, view toggle, board/list views, new task modal"
  artifacts:
    - path: "source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx"
      provides: "Create new task modal with form validation"
      exports: ["NewTaskModal"]
    - path: "source/dashboard/app/frontend/components/tasks/TaskListView.tsx"
      provides: "Table view of tasks"
      exports: ["TaskListView"]
    - path: "source/dashboard/app/frontend/components/pages/TasksPage.tsx"
      provides: "Full Tasks page replacing placeholder"
      exports: ["TasksPage"]
  key_links:
    - from: "source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx"
      to: "source/dashboard/app/frontend/hooks/useTasks.ts"
      via: "useCreateTask mutation"
      pattern: "useCreateTask"
    - from: "source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx"
      to: "source/dashboard/app/frontend/components/ui/Modal.tsx"
      via: "Modal wrapper component"
      pattern: "import.*Modal.*from.*@/components/ui"
    - from: "source/dashboard/app/frontend/components/pages/TasksPage.tsx"
      to: "source/dashboard/app/frontend/hooks/useTasks.ts"
      via: "useTasks for data fetching"
      pattern: "useTasks"
    - from: "source/dashboard/app/frontend/components/pages/TasksPage.tsx"
      to: "source/dashboard/app/frontend/components/tasks/KanbanBoard.tsx"
      via: "KanbanBoard for board view"
      pattern: "import.*KanbanBoard"
---

<objective>
Build the NewTaskModal with form validation, TaskListView table, and compose the full TasksPage replacing the placeholder. This is the final plan that wires everything together into a complete Task Board experience.

Purpose: The New Task modal enables task creation with client-side validation and API mutation. The list view provides a table alternative to the Kanban board. TasksPage orchestrates all components with loading/empty/error states, client-side time filtering, and modal state.

Output: 3 files (NewTaskModal, TaskListView, TasksPage replacement) completing all Phase 6 requirements.
</objective>

<execution_context>
@/Users/vel/development/vlss/et/openclaw-team-manager/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vel/development/vlss/et/openclaw-team-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-task-board/06-RESEARCH.md
@.planning/phases/06-task-board/06-UI-SPEC.md
@.planning/phases/06-task-board/06-01-SUMMARY.md
@.planning/phases/06-task-board/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plans 01-02 and existing codebase -->

From source/dashboard/app/frontend/types/api.ts:
```typescript
export interface Task {
  id: string;
  task_id: string;
  title: string;
  description: string | null;
  status: "backlog" | "queued" | "in_progress" | "awaiting_approval" | "completed" | "failed";
  priority: 0 | 1 | 2 | 3;
  agent_id: string | null;
  agent_name: string | null;
  created_at: string;
  updated_at: string;
}
```

From source/dashboard/app/frontend/hooks/useTasks.ts (after Plan 01):
```typescript
export function useTasks(params?: UseTasksParams): UseQueryResult<PaginatedResponse<Task>>;
export function useCreateTask(): UseMutationResult;
// mutationFn: (data: { task: { task_id: string; title: string; description: string; priority: number; agent_id: string } }) => ...
export function useUpdateTaskStatus(): UseMutationResult;
```

From source/dashboard/app/frontend/hooks/useAgents.ts:
```typescript
export function useAgents(params?: UseAgentsParams): UseQueryResult<PaginatedResponse<Agent>>;
```

From source/dashboard/app/frontend/components/ui/Modal.tsx (from Plan 01):
```typescript
export function Modal({ open, onClose, title, subtitle, children, className }: ModalProps);
```

From source/dashboard/app/frontend/components/tasks/ (from Plans 01-02):
```typescript
export function TaskCard({ task, isDragOverlay }: TaskCardProps);
export function PriorityLegend({ className }: { className?: string });
export function KanbanBoard({ tasks }: KanbanBoardProps);
export function TaskFilters({ timePeriod, onTimePeriodChange }: TaskFiltersProps);
export function TaskViewToggle();
```

From source/dashboard/app/frontend/stores/filterStore.ts:
```typescript
export const useFilterStore: // taskFilters, setTaskFilters, resetTaskFilters
```

From source/dashboard/app/frontend/stores/viewStore.ts:
```typescript
export const useViewStore: // taskView, setTaskView
```

From source/dashboard/app/frontend/components/ui:
```typescript
export function Button({ variant, size, disabled, glow, className, children, ...props }: ButtonProps);
export function Input({ label, helperText, error, icon, className, ...props }: InputProps);
export function Badge({ variant, color, priority, dot, pulse, className, children, ...props }: BadgeProps);
```

From source/dashboard/app/frontend/components/pages/AgentsPage.tsx (page composition pattern):
```typescript
// Uses: loading skeleton (SkeletonCard/SkeletonTable inline components)
// Uses: error state with FontAwesome exclamation icon + Retry button
// Uses: empty state with icon + message + Clear Filters button
// Uses: view toggle conditional rendering
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NewTaskModal with form validation and create mutation</name>
  <files>
    source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx
  </files>
  <read_first>
    source/dashboard/app/frontend/components/ui/Modal.tsx,
    source/dashboard/app/frontend/components/ui/Input.tsx,
    source/dashboard/app/frontend/components/ui/Button.tsx,
    source/dashboard/app/frontend/hooks/useTasks.ts,
    source/dashboard/app/frontend/hooks/useAgents.ts,
    source/dashboard/app/frontend/types/api.ts,
    .planning/phases/06-task-board/06-UI-SPEC.md
  </read_first>
  <action>
**NewTaskModal.tsx** -- Create task modal with form fields, validation, and API mutation.

```typescript
import { useState } from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faPlus, faSpinner } from "@fortawesome/free-solid-svg-icons";
import { toast } from "sonner";
import { cn } from "@/lib/utils";
import { Modal } from "@/components/ui/Modal";
import { Input, Button } from "@/components/ui";
import { useCreateTask } from "@/hooks/useTasks";
import { useAgents } from "@/hooks/useAgents";
```

Props:
```typescript
interface NewTaskModalProps {
  open: boolean;
  onClose: () => void;
}
```

Form state interface:
```typescript
interface NewTaskForm {
  title: string;
  agent_id: string;
  description: string;
  priority: number;
}
```

Initial state (default priority = 2 per NTSK-04):
```typescript
const initialForm: NewTaskForm = { title: "", agent_id: "", description: "", priority: 2 };
```

Implementation:

1. **State:** `const [form, setForm] = useState<NewTaskForm>(initialForm);`
   Also: `const [submitted, setSubmitted] = useState(false);` for showing errors only after first submit attempt.

2. **Hooks:**
   - `const createTask = useCreateTask();`
   - `const { data: agentsData } = useAgents({ per_page: 100 });`
   - `const agents = agentsData?.data ?? [];`

3. **Validation (only show errors after submitted=true):**
```typescript
const errors = {
  title: !form.title.trim() ? "Task name is required" : undefined,
  agent_id: !form.agent_id ? "Agent selection is required" : undefined,
  description: !form.description.trim() ? "Description is required" : undefined,
};
const hasErrors = Object.values(errors).some(Boolean);
```

4. **handleSubmit:**
```typescript
function handleSubmit() {
  setSubmitted(true);
  if (hasErrors) return;
  createTask.mutate(
    {
      task: {
        task_id: `TASK-${Date.now().toString(36).toUpperCase()}`,
        title: form.title.trim(),
        description: form.description.trim(),
        priority: form.priority,
        agent_id: form.agent_id,
      },
    },
    {
      onSuccess: () => {
        toast.success("Task created");
        handleClose();
      },
      onError: () => {
        toast.error("Failed to create task");
      },
    },
  );
}
```

5. **handleClose:** Reset form and submitted state, call `onClose()`.
```typescript
function handleClose() {
  setForm(initialForm);
  setSubmitted(false);
  onClose();
}
```

6. **Render:**
```typescript
<Modal open={open} onClose={handleClose} title="Create New Task" subtitle="Configure and assign a task to an agent.">
  {/* Form fields */}
  <div className="space-y-4">
    {/* Task Name -- use Input component */}
    <Input
      label="Task Name *"
      placeholder="e.g., Scrape competitor pricing data"
      value={form.title}
      onChange={(e) => setForm({ ...form, title: e.target.value })}
      error={submitted ? errors.title : undefined}
      aria-invalid={submitted && !!errors.title}
      aria-describedby={submitted && errors.title ? "title-error" : undefined}
    />

    {/* Agent Dropdown -- styled native select */}
    <div>
      <label className="block text-sm font-semibold text-text-primary mb-1">Assign Agent *</label>
      <select
        className={cn(
          "w-full bg-background border border-border rounded-md py-2 px-3",
          "text-sm text-text-primary",
          "focus:outline-none focus:border-accent focus:ring-1 focus:ring-accent",
          submitted && errors.agent_id && "border-danger",
        )}
        value={form.agent_id}
        onChange={(e) => setForm({ ...form, agent_id: e.target.value })}
        aria-invalid={submitted && !!errors.agent_id}
      >
        <option value="">Select an agent...</option>
        {agents.map((agent) => (
          <option key={agent.id} value={agent.id}>{agent.name}</option>
        ))}
      </select>
      {submitted && errors.agent_id && (
        <p className="mt-1 text-xs text-danger">{errors.agent_id}</p>
      )}
    </div>

    {/* Description Textarea */}
    <div>
      <label className="block text-sm font-semibold text-text-primary mb-1">Description *</label>
      <textarea
        className={cn(
          "w-full bg-background border border-border rounded-md py-2 px-3",
          "text-sm text-text-primary resize-none",
          "focus:outline-none focus:border-accent focus:ring-1 focus:ring-accent",
          submitted && errors.description && "border-danger",
        )}
        rows={4}
        placeholder="Provide detailed instructions for the agent."
        value={form.description}
        onChange={(e) => setForm({ ...form, description: e.target.value })}
        aria-invalid={submitted && !!errors.description}
      />
      {submitted && errors.description && (
        <p className="mt-1 text-xs text-danger">{errors.description}</p>
      )}
    </div>

    {/* Attachment Area -- visual placeholder per NTSK-02 */}
    <div>
      <label className="block text-sm font-semibold text-text-primary mb-1">Attachments</label>
      <div
        className="border-2 border-dashed border-border rounded-lg min-h-[80px] flex items-center justify-center cursor-pointer hover:border-text-secondary transition-colors"
        onClick={() => toast.info("File attachments coming soon")}
        role="button"
        tabIndex={0}
        onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") toast.info("File attachments coming soon"); }}
      >
        <p className="text-xs text-text-secondary">Drag files, context, requirements, or special instructions</p>
      </div>
    </div>

    {/* Priority Level -- button group with role="radiogroup" per UI-SPEC */}
    <div>
      <label className="block text-sm font-semibold text-text-primary mb-1">Priority Level</label>
      <div className="flex gap-2" role="radiogroup" aria-label="Priority Level">
        {([
          { value: 3, label: "Low" },
          { value: 2, label: "Medium" },
          { value: 1, label: "High" },
        ] as const).map((opt) => (
          <button
            key={opt.value}
            type="button"
            role="radio"
            aria-checked={form.priority === opt.value}
            className={cn(
              "px-4 py-2 text-sm rounded-md border transition-colors",
              form.priority === opt.value
                ? "border-accent text-accent bg-accent/10"
                : "border-border text-text-secondary bg-background hover:border-text-secondary",
            )}
            onClick={() => setForm({ ...form, priority: opt.value })}
          >
            {opt.label}
          </button>
        ))}
      </div>
    </div>
  </div>

  {/* Action row */}
  <div className="flex justify-end gap-4 mt-6 pt-4 border-t border-border">
    <Button variant="secondary" onClick={handleClose}>Discard Task</Button>
    <Button
      variant="primary"
      onClick={handleSubmit}
      disabled={createTask.isPending}
    >
      {createTask.isPending ? (
        <><FontAwesomeIcon icon={faSpinner} spin className="mr-2" />Creating...</>
      ) : (
        <><FontAwesomeIcon icon={faPlus} className="mr-2" />Create Task</>
      )}
    </Button>
  </div>
</Modal>
```

Note on priority options: Per UI-SPEC, the design screenshot shows "-", "Low", "Medium", "High". Since P0 (Critical) is not available in the creation form, we offer Low (P3), Medium (P2, default), High (P1). The "-" button is omitted since every task needs a priority.

Export: `export function NewTaskModal({ open, onClose }: NewTaskModalProps)`
  </action>
  <verify>
    <automated>cd /Users/vel/development/vlss/et/openclaw-team-manager/source/dashboard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "export function NewTaskModal"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "useCreateTask"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "useAgents"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "priority: 2" (default medium)
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Task name is required"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Agent selection is required"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Description is required"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Task created"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Failed to create task"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "File attachments coming soon"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "border-dashed"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains 'role="radiogroup"'
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains 'role="radio"'
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "aria-checked"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Discard Task"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Create Task"
    - source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx contains "Create New Task"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>NewTaskModal opens with all fields (name, agent dropdown, description, attachment placeholder, priority group); priority defaults to Medium (2); validation shows errors for empty required fields; create mutation submits to API with toast feedback; attachment area shows placeholder toast; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Build TaskListView and compose full TasksPage</name>
  <files>
    source/dashboard/app/frontend/components/tasks/TaskListView.tsx,
    source/dashboard/app/frontend/components/pages/TasksPage.tsx
  </files>
  <read_first>
    source/dashboard/app/frontend/components/pages/AgentsPage.tsx,
    source/dashboard/app/frontend/components/agents/AgentTable.tsx,
    source/dashboard/app/frontend/components/tasks/KanbanBoard.tsx,
    source/dashboard/app/frontend/components/tasks/TaskFilters.tsx,
    source/dashboard/app/frontend/components/tasks/TaskViewToggle.tsx,
    source/dashboard/app/frontend/components/tasks/PriorityLegend.tsx,
    source/dashboard/app/frontend/components/tasks/NewTaskModal.tsx,
    source/dashboard/app/frontend/hooks/useTasks.ts,
    source/dashboard/app/frontend/stores/filterStore.ts,
    source/dashboard/app/frontend/stores/viewStore.ts,
    source/dashboard/app/frontend/types/api.ts,
    .planning/phases/06-task-board/06-UI-SPEC.md
  </read_first>
  <action>
**TaskListView.tsx** -- Table view for tasks, replicating AgentTable pattern with sortable columns.

```typescript
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faSort, faSortUp, faSortDown } from "@fortawesome/free-solid-svg-icons";
import { formatDistanceToNow } from "date-fns";
import { cn } from "@/lib/utils";
import { Badge } from "@/components/ui";
import type { Task } from "@/types/api";
```

Props:
```typescript
interface TaskListViewProps {
  tasks: Task[];
  sort?: string;
  dir?: "asc" | "desc";
  onSortChange: (column: string) => void;
}
```

Column definitions:
```typescript
const columns = [
  { key: "task_id", label: "ID", sortable: false, width: "w-24" },
  { key: "title", label: "Title", sortable: true, width: "w-auto" },
  { key: "status", label: "Status", sortable: true, width: "w-32" },
  { key: "priority", label: "Priority", sortable: true, width: "w-24" },
  { key: "agent_name", label: "Agent", sortable: false, width: "w-32", hiddenBelow: "md" },
  { key: "created_at", label: "Created", sortable: true, width: "w-32", hiddenBelow: "lg" },
];
```

Implementation:
- Container: `bg-surface rounded-lg border border-border overflow-x-auto`
- Table: `w-full text-sm`
- Table head: `bg-background/50` with columns, each sortable column has click handler and sort icon (faSort / faSortUp / faSortDown), `aria-sort` attribute
- Table body rows: `hover:bg-surface-hover/30 transition-colors border-t border-border`
- Columns:
  - ID: `<span className="font-mono text-xs text-text-secondary">{task.task_id}</span>`
  - Title: `<span className="text-text-primary font-semibold">{task.title}</span>`
  - Status: `<Badge variant="status" color={statusColorMap[task.status]}>{statusLabel[task.status]}</Badge>` where statusColorMap maps task status to Badge color and statusLabel maps to display text
  - Priority: `<Badge variant="priority" priority={priorityBadgeMap[task.priority]}>{priorityLabel[task.priority]}</Badge>`
  - Agent: `task.agent_name || "Unassigned"` in text-text-secondary
  - Created: `formatDistanceToNow(new Date(task.created_at), { addSuffix: true })` in font-mono text-xs text-text-secondary
- Hidden column handling: use `hiddenBelow` to add responsive hiding classes (e.g., `hidden md:table-cell`)

Status to Badge color map:
```typescript
const statusColorMap: Record<Task["status"], "success" | "danger" | "warning" | "info" | "idle"> = {
  backlog: "idle",
  queued: "info",
  in_progress: "warning",
  awaiting_approval: "warning",
  completed: "success",
  failed: "danger",
};
const statusLabel: Record<Task["status"], string> = {
  backlog: "Backlog", queued: "Queued", in_progress: "In Progress",
  awaiting_approval: "Awaiting Approval", completed: "Completed", failed: "Failed",
};
```

Priority label map:
```typescript
const priorityLabel: Record<number, string> = { 0: "P0", 1: "P1", 2: "P2", 3: "P3" };
const priorityBadgeMap: Record<number, "p0" | "p1" | "p2" | "p3"> = { 0: "p0", 1: "p1", 2: "p2", 3: "p3" };
```

Sort icon helper function (same pattern as AgentTable):
```typescript
function getSortIcon(column: string) {
  if (sort !== column) return faSort;
  return dir === "asc" ? faSortUp : faSortDown;
}
```

Export: `export function TaskListView({ tasks, sort, dir, onSortChange }: TaskListViewProps)`

**TasksPage.tsx** -- Full page composition replacing the placeholder.

```typescript
import { useState, useMemo } from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import {
  faPlus,
  faExclamationTriangle,
  faListCheck,
} from "@fortawesome/free-solid-svg-icons";
import { isAfter, subDays, subHours } from "date-fns";
import { useTasks } from "@/hooks/useTasks";
import { useFilterStore } from "@/stores/filterStore";
import { useViewStore } from "@/stores/viewStore";
import { Button } from "@/components/ui";
import { KanbanBoard } from "@/components/tasks/KanbanBoard";
import { TaskListView } from "@/components/tasks/TaskListView";
import { TaskFilters } from "@/components/tasks/TaskFilters";
import { TaskViewToggle } from "@/components/tasks/TaskViewToggle";
import { PriorityLegend } from "@/components/tasks/PriorityLegend";
import { NewTaskModal } from "@/components/tasks/NewTaskModal";
import type { Task } from "@/types/api";
```

State:
```typescript
const [modalOpen, setModalOpen] = useState(false);
const [timePeriod, setTimePeriod] = useState("all");
const [sort, setSort] = useState<string | undefined>(undefined);
const [dir, setDir] = useState<"asc" | "desc" | undefined>(undefined);
```

Stores:
```typescript
const { taskFilters, resetTaskFilters } = useFilterStore();
const { taskView } = useViewStore();
```

Data fetching -- fetch all tasks (per_page=100) for board view grouping:
```typescript
const { data, isLoading, isError, refetch } = useTasks({
  ...taskFilters,
  per_page: 100,
  sort,
  dir,
});
```

Client-side time period filter:
```typescript
function filterByTimePeriod(tasks: Task[], period: string): Task[] {
  if (period === "all") return tasks;
  const now = new Date();
  const thresholds: Record<string, Date> = {
    "24h": subHours(now, 24),
    "7d": subDays(now, 7),
    "30d": subDays(now, 30),
  };
  const cutoff = thresholds[period];
  if (!cutoff) return tasks;
  return tasks.filter((t) => isAfter(new Date(t.created_at), cutoff));
}

const allTasks = data?.data ?? [];
const filteredTasks = useMemo(() => filterByTimePeriod(allTasks, timePeriod), [allTasks, timePeriod]);
```

Sort handler (3-state toggle, same pattern as AgentsPage):
```typescript
function handleSortChange(column: string) {
  if (sort === column) {
    if (dir === "asc") {
      setDir("desc");
    } else {
      setSort(undefined);
      setDir(undefined);
    }
  } else {
    setSort(column);
    setDir("asc");
  }
}

function handleClearFilters() {
  resetTaskFilters();
  setTimePeriod("all");
}
```

Skeleton components (inline, following AgentsPage pattern):

**SkeletonBoard:**
```typescript
function SkeletonBoard() {
  return (
    <div className="flex gap-2 overflow-x-auto pb-2">
      {Array.from({ length: 6 }).map((_, i) => (
        <div key={i} className="flex-shrink-0 w-[280px] min-h-[200px] rounded-lg p-2 bg-background/50 animate-pulse">
          <div className="bg-surface-hover/50 rounded h-4 w-24 mb-4 mx-2" />
          {Array.from({ length: 2 + (i % 2) }).map((_, j) => (
            <div key={j} className="bg-surface rounded-lg border border-border p-3 mb-2">
              <div className="bg-surface-hover/50 rounded h-3 w-16 mb-2" />
              <div className="bg-surface-hover/50 rounded h-4 w-full mb-2" />
              <div className="bg-surface-hover/50 rounded h-3 w-3/4 mb-2" />
              <div className="bg-surface-hover/50 rounded h-3 w-20" />
            </div>
          ))}
        </div>
      ))}
    </div>
  );
}
```

**SkeletonTable:**
```typescript
function SkeletonTable() {
  return (
    <div className="bg-surface rounded-lg border border-border p-6 animate-pulse">
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} className="h-4 bg-surface-hover/50 rounded w-full mb-3" />
      ))}
    </div>
  );
}
```

Render:
```tsx
<div>
  {/* Header row: title + subtitle on left, view toggle + new task button on right */}
  <div className="flex items-center justify-between">
    <div>
      <h1 className="text-xl font-semibold text-text-primary">Task Board</h1>
      <p className="text-sm text-text-secondary mt-1">Monitor and manage agent workflows across all stages.</p>
    </div>
    <div className="flex items-center gap-3">
      <TaskViewToggle />
      <Button variant="primary" onClick={() => setModalOpen(true)}>
        <FontAwesomeIcon icon={faPlus} className="mr-2" />
        New Task
      </Button>
    </div>
  </div>

  {/* Filters + Priority Legend */}
  <div className="mt-6 flex items-center justify-between flex-wrap gap-4">
    <TaskFilters timePeriod={timePeriod} onTimePeriodChange={setTimePeriod} />
    <PriorityLegend />
  </div>

  {/* Content */}
  <div className="mt-4">
    {isLoading && (taskView === "list" ? <SkeletonTable /> : <SkeletonBoard />)}

    {isError && (
      <div className="flex flex-col items-center justify-center py-16">
        <FontAwesomeIcon icon={faExclamationTriangle} className="text-danger opacity-50 mb-4" style={{ fontSize: 48 }} />
        <h2 className="text-lg font-semibold text-text-primary mb-2">Failed to load tasks</h2>
        <p className="text-sm text-text-secondary max-w-md text-center mb-6">
          Unable to fetch task data from the server. Check that the Rails server is running and try again.
        </p>
        <Button variant="primary" onClick={() => refetch()}>Retry</Button>
      </div>
    )}

    {!isLoading && !isError && filteredTasks.length === 0 && (
      <div className="flex flex-col items-center justify-center py-16">
        <FontAwesomeIcon icon={faListCheck} className="text-text-secondary opacity-50 mb-4" style={{ fontSize: 48 }} />
        <h2 className="text-lg font-semibold text-text-primary mb-2">
          {taskView === "board" ? "No tasks yet" : "No tasks found"}
        </h2>
        <p className="text-sm text-text-secondary max-w-md text-center mb-6">
          {taskView === "board"
            ? 'Create your first task to get started. Click "New Task" to assign work to an agent.'
            : "No tasks match the current filters. Try adjusting your filter criteria or create a new task."
          }
        </p>
        <Button variant="secondary" onClick={handleClearFilters}>Clear Filters</Button>
      </div>
    )}

    {!isLoading && !isError && filteredTasks.length > 0 && (
      taskView === "list" ? (
        <TaskListView tasks={filteredTasks} sort={sort} dir={dir} onSortChange={handleSortChange} />
      ) : (
        <KanbanBoard tasks={filteredTasks} />
      )
    )}
  </div>

  {/* New Task Modal */}
  <NewTaskModal open={modalOpen} onClose={() => setModalOpen(false)} />
</div>
```

Export: `export function TasksPage()` (named export, same pattern as existing).
  </action>
  <verify>
    <automated>cd /Users/vel/development/vlss/et/openclaw-team-manager/source/dashboard && npx tsc --noEmit && bundle exec rspec spec/requests/api/v1/tasks_spec.rb -f progress</automated>
  </verify>
  <acceptance_criteria>
    - source/dashboard/app/frontend/components/tasks/TaskListView.tsx contains "export function TaskListView"
    - source/dashboard/app/frontend/components/tasks/TaskListView.tsx contains "onSortChange"
    - source/dashboard/app/frontend/components/tasks/TaskListView.tsx contains "aria-sort"
    - source/dashboard/app/frontend/components/tasks/TaskListView.tsx contains "formatDistanceToNow"
    - source/dashboard/app/frontend/components/tasks/TaskListView.tsx contains "statusColorMap"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "export function TasksPage"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx does NOT contain "PlaceholderSkeleton"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "KanbanBoard"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "TaskListView"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "TaskFilters"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "TaskViewToggle"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "PriorityLegend"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "NewTaskModal"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "per_page: 100"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "Failed to load tasks"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "No tasks yet"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "filterByTimePeriod"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "SkeletonBoard"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "setModalOpen(true)"
    - source/dashboard/app/frontend/components/pages/TasksPage.tsx contains "New Task"
    - `npx tsc --noEmit` exits 0
    - `bundle exec rspec spec/requests/api/v1/tasks_spec.rb` exits 0
  </acceptance_criteria>
  <done>TaskListView renders sortable table with status/priority badges; TasksPage replaces placeholder with full page composition: header with New Task button, filters bar, priority legend, board/list toggle, loading/error/empty states, Kanban board or table view, and NewTaskModal; all specs pass; TypeScript compiles</done>
</task>

</tasks>

<verification>
- `cd source/dashboard && npx tsc --noEmit` -- zero TypeScript errors
- `cd source/dashboard && bundle exec rspec` -- full suite passes
- TasksPage no longer imports PlaceholderSkeleton
- All 11 requirement IDs covered across all 3 plans
</verification>

<success_criteria>
- NewTaskModal has form with name, agent dropdown, description, attachment placeholder, priority group
- Priority defaults to Medium (2), validation blocks empty required fields
- Create mutation uses useCreateTask with toast feedback
- Attachment area shows "File attachments coming soon" toast on click
- TaskListView shows sortable table with ID, title, status, priority, agent, created columns
- TasksPage replaces placeholder, renders header with "+ New Task" button, filters, priority legend, view toggle
- Board view shows KanbanBoard, list view shows TaskListView
- Loading shows skeleton board or table, error shows retry, empty shows contextual message
- Time period filter works client-side (All, 24h, 7d, 30d)
- Full RSpec suite passes
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-board/06-03-SUMMARY.md`
</output>
